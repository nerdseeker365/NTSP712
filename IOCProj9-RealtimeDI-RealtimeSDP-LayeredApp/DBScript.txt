CREATE TABLE "SYSTEM"."LAYERED_CUSTOMER" 
   (	"CUSTNO" NUMBER(10,0) NOT NULL ENABLE, 
	"CUSTNAME" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	"COLUMN1" VARCHAR2(20 BYTE), 
	"PAMT" FLOAT(126), 
	"INTRAMT" FLOAT(126), 
	 CONSTRAINT "LAYERED_CUSTOMER_PK" PRIMARY KEY ("CUSTNO"))